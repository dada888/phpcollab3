dev:
  confing_comments_plugin:
    pager_max_per_page: 10

all:
  confing_comments_plugin:
    Pager:
      max_per_page: 10
    Profile:
      enabled: true
      class_name: Profile
      class_id:   id